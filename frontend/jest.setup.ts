/**
 * Jest DOM Setup
 * 
 * Extends Jest's expect with @testing-library/jest-dom matchers.
 * This enables matchers like toBeInTheDocument(), toHaveAttribute(), etc.
 */
import '@testing-library/jest-dom';
